// Event names are free-form strings from the API (e.g. "invoice_sent",
// "invoice_paid_with_thank_you_note", "inbox_file_added", etc.).
// No enum is needed; the Event model uses String? for the name field.
